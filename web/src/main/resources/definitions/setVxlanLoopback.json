{
    "type": "object",
    "required": [ "id" ],
    "properties": {
        "id": {
            "type": "string",
            "description": "source-loopback ID in switch for VXLAN",
            "example": "0"
        }
    }
}