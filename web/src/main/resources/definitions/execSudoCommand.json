{
    "type": "object",
    "required": [ "command" ],
    "properties": {
        "scommand": {
            "type": "string",
            "description": "privileged command",
            "example": "ovs-vsctl show"
        }
    }
}