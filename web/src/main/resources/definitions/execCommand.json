{
    "type": "object",
    "required": [ "command" ],
    "properties": {
        "command": {
            "type": "string",
            "description": "non-privileged command",
            "example": "echo 123"
        }
    }
}