{
    "type": "object",
    "required": [ "vnid", "vid" ],
    "properties": {
        "vnid": {
            "type": "string",
            "description": "VNI",
            "example": "2000"
        },
        "vid": {
            "type": "string",
            "description": "VLAN ID",
            "example": "10"
        }
    }
}